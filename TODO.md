# GlueClaw TODO

## Nice to have

- **Tool activity indicators** - TUI shows spinner but no "Running Bash..." status. Would need dist patch to inject events into gateway event bus.

## Future

- **Auto-fix detection** - healthcheck cron that binary searches and patches new Anthropic triggers automatically
- **npm publish** - `openclaw plugins install glueclaw` instead of git clone
